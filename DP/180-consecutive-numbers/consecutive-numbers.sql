# Write your MySQL query statement below
select distinct num as ConsecutiveNums from (select num,lag(num,1) over (order by id) as l,lag(num,2) over (order by id) as ll from Logs)temp where num=l and num=ll;